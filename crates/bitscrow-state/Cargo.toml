# File: crates/bitscrow-state/Cargo.toml
# Purpose: Offline lifecycle FSM + StateRevision digests (Phase 2).
# Related: bitscrow-spec, docs/system_architecture.md §4
# Tags: #crate #state #phase2
[package]
name = "bitscrow-state"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "BITSCROW v0.1 offline state machine and state-hash digests"
publish = false

[dependencies]
bitscrow-spec = { path = "../bitscrow-spec" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "2.0"

[dev-dependencies]
